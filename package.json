{
  "name": "song-request",
  "version": "1.1.0",
  "private": true,
  "description": "Integration addon for StreamKit+.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@rocketman-streamkit/types": "^1.0.45",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
